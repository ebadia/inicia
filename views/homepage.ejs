<div ng-app="iniciaApp" ng-controller="AppCtrl as vm">

  <div class="container">
    <div class="row">
      <div class="col-md-12">

        <h1>Inicia is starting big...</h1>
        <ul>
          <li ng-repeat="user in users">
            <span>{{user.name}}</span>
          </li>
        </ul>

      </div>
    </div>
  </div>

</div>
